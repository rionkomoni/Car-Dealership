apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: car-dealership
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: ClusterIP
