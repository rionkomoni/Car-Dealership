apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "car-dealership.fullname" . }}-backend-config
data:
{{- range $key, $val := .Values.backend.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
